import { FC, PropsWithChildren } from "react";
import { IMeta } from "./meta.interface";

const getTitile = (title: string) => `${title} | Test website catalog`

const Meta: FC<PropsWithChildren<IMeta>> = () => {
    return <>
        
    </>
}

export default Meta;